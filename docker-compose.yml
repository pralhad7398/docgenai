version: '3.9'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db_data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  db_data:
